# GitHub Actions workflow for building OpenWrt firmware for CMCC PZ-L8
# Target: AP mode only with mesh support
# Features: Optimized caching for faster builds, WiFi BDF from firmware_qca-wireless PR #106

name: Build OpenWrt for CMCC PZ-L8 (AP Mode)

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag (leave empty for no release)'
        required: false
        default: ''
  push:
    tags:
      - 'v*'

env:
  TARGET: qualcommax
  SUBTARGET: ipq50xx
  DEVICE: cmcc_pz-l8

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-22.04
    timeout-minutes: 720

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 2048
          swap-size-mb: 1024
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git \
            libncurses-dev libssl-dev python3-distutils \
            python3-setuptools python3-dev rsync swig unzip \
            zlib1g-dev file wget curl jq device-tree-compiler \
            ccache libelf-dev libdw-dev libiberty-dev

      - name: Checkout OpenWrt
        uses: actions/checkout@v4
        with:
          repository: openwrt/openwrt
          ref: main
          path: openwrt
          fetch-depth: 0

      - name: Fetch PR #21495 (PZ-L8 device support + ath11k-smallbuffers)
        working-directory: openwrt
        run: |
          git fetch origin pull/21495/head:pr-21495
          git config user.email "builder@github.com"
          git config user.name "GitHub Actions"
          git merge pr-21495 --no-edit

      - name: Download BDF files for PZ-L8 WiFi
        working-directory: openwrt
        run: |
          # BDF (Board Data Files) are required for ath11k WiFi driver
          # These files contain calibration data specific to this device
          # Source: firmware_qca-wireless PR #106 (extracted from official 501.11 firmware)

          # Create directories for board-2.bin files
          # These will be included in the firmware image via the files/ directory
          mkdir -p files/lib/firmware/ath11k/IPQ5018/hw1.0
          mkdir -p files/lib/firmware/ath11k/QCN6122/hw1.0

          # Download BDF files
          # IPQ5000 (2.4GHz SoC built-in WiFi) → board-2.bin for IPQ5018
          curl -L -o files/lib/firmware/ath11k/IPQ5018/hw1.0/board-2.bin \
            "https://github.com/openwrt/firmware_qca-wireless/raw/f7ad5fee1924efdb5d8b2d1bf95bd3867d22e701/board-cmcc_pz-l8.ipq5018"

          # QCN6102 (5GHz external WiFi module) → board-2.bin for QCN6122
          curl -L -o files/lib/firmware/ath11k/QCN6122/hw1.0/board-2.bin \
            "https://github.com/openwrt/firmware_qca-wireless/raw/f7ad5fee1924efdb5d8b2d1bf95bd3867d22e701/board-cmcc_pz-l8.qcn6122"

          # Verify downloads
          echo "=== Downloaded BDF files ==="
          ls -la files/lib/firmware/ath11k/IPQ5018/hw1.0/
          ls -la files/lib/firmware/ath11k/QCN6122/hw1.0/

          # Check file sizes (should be ~131KB each)
          SIZE_IPQ=$(stat -c%s "files/lib/firmware/ath11k/IPQ5018/hw1.0/board-2.bin")
          SIZE_QCN=$(stat -c%s "files/lib/firmware/ath11k/QCN6122/hw1.0/board-2.bin")

          echo "IPQ5018 BDF size: $SIZE_IPQ bytes (expected: 131176)"
          echo "QCN6122 BDF size: $SIZE_QCN bytes (expected: 131176)"

          if [ "$SIZE_IPQ" -lt 100000 ] || [ "$SIZE_QCN" -lt 100000 ]; then
            echo "ERROR: BDF files appear to be incomplete or corrupted"
            exit 1
          fi

          echo "BDF files downloaded successfully!"
          echo "These will be installed directly to /lib/firmware/ath11k/ in the image"

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-${{ env.TARGET }}-v4-${{ github.ref_name }}-${{ hashFiles('openwrt/feeds.conf.default') }}
          restore-keys: |
            ccache-${{ env.TARGET }}-v4-${{ github.ref_name }}-
            ccache-${{ env.TARGET }}-v4-

      - name: Cache toolchain
        uses: actions/cache@v4
        with:
          path: openwrt/staging_dir
          key: toolchain-${{ env.TARGET }}-v4-${{ hashFiles('openwrt/include/version.mk') }}
          restore-keys: toolchain-${{ env.TARGET }}-v4-

      # Do NOT cache build_dir to ensure ipq-wifi is rebuilt with BDF files

      - name: Configure ccache
        run: |
          ccache --set-config=cache_dir=~/.ccache
          ccache --set-config=max_size=10G
          ccache --set-config=compression=true
          ccache -z

      - name: Update feeds
        working-directory: openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Create custom files for AP mode
        working-directory: openwrt
        run: |
          # Create custom files directory
          mkdir -p files/etc/uci-defaults
          mkdir -p files/etc/config

          # Network configuration for AP mode
          # All ports (lan1, lan2, lan3, wan) bridged together
          # DHCP client with static fallback on 192.168.10.1
          cat > files/etc/config/network << 'EOF'
          config interface 'loopback'
              option device 'lo'
              option proto 'static'
              option ipaddr '127.0.0.1'
              option netmask '255.0.0.0'

          config device
              option name 'br-lan'
              option type 'bridge'
              list ports 'lan1'
              list ports 'lan2'
              list ports 'lan3'
              list ports 'wan'

          config interface 'lan'
              option device 'br-lan'
              option proto 'dhcp'

          config interface 'lan_static'
              option device 'br-lan'
              option proto 'static'
              option ipaddr '192.168.10.1'
              option netmask '255.255.255.0'
          EOF

          # uci-defaults script to ensure AP mode network config on first boot
          cat > files/etc/uci-defaults/99-ap-mode-network << 'EOF'
          #!/bin/sh
          # AP Mode Network Configuration for CMCC PZ-L8
          # Bridges all ports (lan1, lan2, lan3, wan) together

          uci -q batch << 'UCI'
          delete network.@device[0]
          delete network.lan
          delete network.wan
          delete network.wan6

          set network.br_lan='device'
          set network.br_lan.name='br-lan'
          set network.br_lan.type='bridge'
          add_list network.br_lan.ports='lan1'
          add_list network.br_lan.ports='lan2'
          add_list network.br_lan.ports='lan3'
          add_list network.br_lan.ports='wan'

          set network.lan='interface'
          set network.lan.device='br-lan'
          set network.lan.proto='dhcp'

          set network.lan_static='interface'
          set network.lan_static.device='br-lan'
          set network.lan_static.proto='static'
          set network.lan_static.ipaddr='192.168.10.1'
          set network.lan_static.netmask='255.255.255.0'
          UCI

          uci commit network
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-ap-mode-network

          # IPv6 configuration for AP mode
          # Enable receiving Router Advertisements to get IPv6 address via SLAAC
          # Disable forwarding so kernel accepts RA (forwarding=1 means router mode)
          mkdir -p files/etc/sysctl.d
          cat > files/etc/sysctl.d/99-ipv6-ap.conf << 'EOF'
          # IPv6 AP Mode Configuration
          # Allow br-lan to receive Router Advertisements
          net.ipv6.conf.br-lan.accept_ra = 2
          # Disable forwarding to accept RA (forwarding=1 = router mode, won't accept RA)
          net.ipv6.conf.br-lan.forwarding = 0
          # Also set default values for any interface
          net.ipv6.conf.default.accept_ra = 2
          net.ipv6.conf.default.forwarding = 0
          EOF

      - name: Generate configuration
        working-directory: openwrt
        run: |
          # Create our custom config
          cat > .config << 'EOF'
          # Target
          CONFIG_TARGET_qualcommax=y
          CONFIG_TARGET_qualcommax_ipq50xx=y
          CONFIG_TARGET_qualcommax_ipq50xx_DEVICE_cmcc_pz-l8=y

          # Build
          CONFIG_DEVEL=y
          CONFIG_CCACHE=y
          CONFIG_USE_CCACHE=y
          CONFIG_ALL_KMODS=y
          CONFIG_ALL_NONSHARED=y

          # Image
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_TARGET_IMAGES_GZIP=y

          # Include custom files
          CONFIG_INCLUDE_CONFIG=y

          # Kernel
          CONFIG_KERNEL_CRASHLOG=y
          CONFIG_KERNEL_SWAP=y
          CONFIG_KERNEL_DEBUG_FS=y

          # Disable router components - use =m to compile but NOT install to image
          # This is the reliable way to exclude default packages
          CONFIG_PACKAGE_dnsmasq=m
          CONFIG_PACKAGE_odhcpd=m
          CONFIG_PACKAGE_odhcpd-ipv6only=m
          CONFIG_PACKAGE_odhcp6c=m
          CONFIG_PACKAGE_firewall4=m
          CONFIG_PACKAGE_nftables=m
          CONFIG_PACKAGE_kmod-nft-offload=m
          CONFIG_PACKAGE_ppp=m
          CONFIG_PACKAGE_ppp-mod-pppoe=m
          CONFIG_PACKAGE_luci-app-firewall=m
          CONFIG_PACKAGE_luci-proto-ipv6=m
          CONFIG_PACKAGE_luci-proto-ppp=m

          # mbedtls
          CONFIG_PACKAGE_libustream-mbedtls=y

          # WiFi - ath11k-smallbuffers for 256MB RAM devices
          CONFIG_PACKAGE_kmod-ath11k-ahb=y
          CONFIG_PACKAGE_kmod-ath11k-smallbuffers=y
          CONFIG_PACKAGE_ath11k-firmware-ipq5018-qcn6122=y
          # BDF files are included directly via files/ directory, not via ipq-wifi package
          CONFIG_PACKAGE_ipq-wifi-cmcc_pz-l8=m
          CONFIG_PACKAGE_wpad-mesh-mbedtls=y

          # Mesh
          CONFIG_PACKAGE_usteer=y
          CONFIG_PACKAGE_luci-app-usteer=y

          # LuCI minimal
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_luci-mod-admin-full=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-app-package-manager=y
          CONFIG_PACKAGE_uhttpd=y
          CONFIG_PACKAGE_uhttpd-mod-ubus=y
          CONFIG_PACKAGE_luci=m

          # Core
          CONFIG_PACKAGE_apk-mbedtls=y
          CONFIG_PACKAGE_base-files=y
          CONFIG_PACKAGE_ca-bundle=y
          CONFIG_PACKAGE_dropbear=y
          CONFIG_PACKAGE_fstools=y
          CONFIG_PACKAGE_kmod-gpio-button-hotplug=y
          CONFIG_PACKAGE_kmod-leds-gpio=y
          CONFIG_PACKAGE_kmod-qca-nss-dp=y
          CONFIG_PACKAGE_logd=y
          CONFIG_PACKAGE_mtd=y
          CONFIG_PACKAGE_netifd=y
          CONFIG_PACKAGE_procd-ujail=y
          CONFIG_PACKAGE_uboot-envtools=y
          CONFIG_PACKAGE_uci=y
          CONFIG_PACKAGE_urandom-seed=y
          CONFIG_PACKAGE_urngd=y
          CONFIG_PACKAGE_zram-swap=y
          CONFIG_PACKAGE_iw=y
          CONFIG_PACKAGE_iwinfo=y
          EOF

          # Run defconfig twice to normalize and resolve dependencies
          make defconfig
          make defconfig

          # Show final package selections
          echo "=== Final package configuration ==="
          grep -E 'CONFIG_PACKAGE_(odhcp|dnsmasq|firewall|ppp)' .config || true
          echo "==================================="

      - name: Download sources
        working-directory: openwrt
        run: |
          # Retry download up to 3 times with increasing delay
          for i in 1 2 3; do
            echo "Download attempt $i/3..."
            if make download -j$(nproc) V=s; then
              echo "Download successful!"
              break
            fi
            if [ $i -eq 3 ]; then
              echo "All download attempts failed!"
              exit 1
            fi
            echo "Download failed, waiting 30s before retry..."
            sleep 30
          done

      - name: Build
        working-directory: openwrt
        run: make -j$(nproc) V=w || make -j1 V=s

      - name: Show ccache stats
        run: ccache -s

      - name: Verify BDF files are in place
        working-directory: openwrt
        run: |
          echo "=== Verifying BDF files in files/ directory ==="
          ls -la files/lib/firmware/ath11k/IPQ5018/hw1.0/
          ls -la files/lib/firmware/ath11k/QCN6122/hw1.0/
          echo "==============================================="

      - name: Prepare artifacts
        run: |
          mkdir -p release
          find openwrt/bin/targets -name "*.bin" -exec cp {} release/ \;
          find openwrt/bin/targets -name "*.img.gz" -exec cp {} release/ \;
          cd release && sha256sum * > sha256sums

          cat > BUILD_INFO.txt << EOF
          Build: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          Target: ${{ env.TARGET }}/${{ env.SUBTARGET }}
          Device: ${{ env.DEVICE }}
          Mode: AP Only with Mesh
          PR: #21495 applied (ath11k-smallbuffers)
          BDF: firmware_qca-wireless PR #106 (from official 501.11 firmware)

          Network Configuration:
          - All ports (lan1, lan2, lan3, wan) bridged to br-lan
          - DHCP client enabled
          - Static fallback: 192.168.10.1

          WiFi:
          - 2.4GHz: IPQ5000 (SoC built-in)
          - 5GHz: QCN6102
          - Driver: ath11k-smallbuffers (optimized for 256MB RAM)
          EOF

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-${{ env.DEVICE }}-${{ github.run_number }}
          path: release/
          retention-days: 30

      - name: Create release
        if: inputs.release_tag != '' || startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ inputs.release_tag || github.ref_name }}
          files: release/*
          body: |
            OpenWrt Firmware for CMCC PZ-L8 (AP Mode)

            ## Features
            - AP Mode Only
            - Mesh support (802.11s)
            - usteer for roaming
            - ath11k-smallbuffers for low-RAM optimization (256MB)
            - WiFi BDF from official firmware 501.11

            ## Network Configuration
            - All ports (lan1, lan2, lan3, wan) bridged together
            - DHCP client enabled
            - Static fallback IP: 192.168.10.1

            ## WiFi
            - 2.4GHz: IPQ5000 (SoC built-in)
            - 5GHz: QCN6102

            ## Known Issues
            - First boot may take longer due to uci-defaults execution
            - WiFi may need a few seconds to initialize after boot
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
